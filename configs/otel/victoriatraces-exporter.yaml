extensions:
  health_check:

receivers:
  prometheus/feedback:
    config:
      scrape_configs:
        - job_name: 'victoriatraces-exporter'
          scrape_interval: 10s
          static_configs:
            - targets: ['127.0.0.1:8888']

  kafka/traces:
    protocol_version: 2.1.0
    brokers:
      - kafka:9092
    traces:
      topics:
        - otlp_spans
    group_id: victoriatraces-exporter
    message_marking:
      after: true
      on_error: false

exporters:
  otlp/feedback:
    endpoint: opentelemetry-collector:4317
    tls:
      insecure: true

  otlphttp/victoriatraces:
    traces_endpoint: http://victoriatraces:10428/insert/opentelemetry/v1/traces
    sending_queue:
      enabled: true
      queue_size: 10000
      num_consumers: 10
    retry_on_failure:
      enabled: true
      initial_interval: 1s
      max_interval: 30s
      max_elapsed_time: 0s

service:
  extensions: [health_check]
  pipelines:
    metrics/feedback:
      receivers: [prometheus/feedback]
      processors: []
      exporters: [otlp/feedback]
    traces:
      receivers: [kafka/traces]
      processors: []
      exporters: [otlphttp/victoriatraces]
